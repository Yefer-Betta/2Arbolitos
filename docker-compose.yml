services:
  db:
    image: mysql:8.0
    container_name: 2arbolitos-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?Variable MYSQL_ROOT_PASSWORD requerida}
      MYSQL_DATABASE: 2arbolitos
      MYSQL_CHARACTER_SET_SERVER: utf8mb4
      MYSQL_COLLATION_SERVER: utf8mb4_unicode_ci
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - 2arbolitos-net

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: 2arbolitos-backend
    restart: unless-stopped
    environment:
      PORT: 3002
      NODE_ENV: production
      DATABASE_URL: mysql://root:${MYSQL_ROOT_PASSWORD}@db:3306/2arbolitos?schema=public&charset=utf8mb4
      JWT_SECRET: ${JWT_SECRET:?Variable JWT_SECRET requerida}
      JWT_EXPIRES_IN: 7d
      HOST_IP: ${HOST_IP:-0.0.0.0}
      DOCKER: "true"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - 2arbolitos-net

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: 2arbolitos-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-80}:80"
    depends_on:
      - backend
    networks:
      - 2arbolitos-net

volumes:
  mysql_data:

networks:
  2arbolitos-net:
    driver: bridge
